kestra:
  storage:
    type: local
    local:
      base-path: /tmp/unittest
  queue:
    type: memory
  repository:
    type: memory
  worker:
    controllers:
      type: STATIC
      static:
        endpoints:
          - host: localhost
