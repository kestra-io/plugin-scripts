dependencies {
    implementation project(":scripts")

    api group: 'org.codehaus.groovy', name: 'groovy-jsr223', version: '3.0.7'

    testImplementation project(':scripts').sourceSets.test.output
}
